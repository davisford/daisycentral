{ key: { val: [4,5], fake: true }, name: 'aaron', idontexist: true }

// must walk each path of the schema
// use value from obj if available
// values in obj but not in schema must be retained? nah we should be ok with
// atomic ops.
schema: {
    key: { val: [Number]}
  , name: { type: String, default: '-empty-' }
}


